language: cpp

compiler:
  - gcc

before_install:
  - git clone https://github.com/eddyxu/cpp-coveralls.git
  - cd cpp-coveralls
  - sudo python setup.py install
  - cd ..

script:
  - cmake . && make 
  - ./prog1
   

after_success:
  - coveralls --verbose
